national_rail:
  outputs:
    dev:
      dataset: your_bigquery_dataset
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: path/to/the/service/account/key/json
      location: region
      method: service-account
      priority: interactive
      project: your_google_cloud_project
      threads: 1
      type: bigquery
  target: dev